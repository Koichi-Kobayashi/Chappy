<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cm="clr-namespace:Chappy.Wpf.Controls.ContextMenu">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <!--  ===== Files-like =====  -->
    <SolidColorBrush x:Key="Files.Menu.Bg" Color="#F2F2F2" />
    <SolidColorBrush x:Key="Files.Menu.Border" Color="#22000000" />
    <SolidColorBrush x:Key="Files.Menu.Hover" Color="#14000000" />
    <SolidColorBrush x:Key="Files.Menu.Text" Color="#FF111111" />
    <SolidColorBrush x:Key="Files.Menu.SubText" Color="#7F000000" />
    <SolidColorBrush x:Key="Files.Menu.Separator" Color="#1A000000" />

    <!--  ShellContextMenuDataGrid は “普通の DataGrid の見た目” を継承  -->
    <Style BasedOn="{StaticResource {x:Type DataGrid}}" TargetType="{x:Type cm:ShellContextMenuDataGrid}" />

    <Style x:Key="Files.MenuButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        x:Name="bd"
                        Background="{TemplateBinding Background}"
                        CornerRadius="8">
                        <ContentPresenter Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Files.Menu.Hover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#22000000" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="Files.IconButton"
        BasedOn="{StaticResource Files.MenuButton}"
        TargetType="{x:Type Button}">
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="10,8" />
    </Style>

    <Style TargetType="{x:Type cm:Win11ContextMenuView}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type cm:Win11ContextMenuView}">
                    <Border
                        Background="{StaticResource Files.Menu.Bg}"
                        BorderBrush="{StaticResource Files.Menu.Border}"
                        BorderThickness="1"
                        CornerRadius="12"
                        SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="18" Opacity="0.18" />
                        </Border.Effect>

                        <Grid Margin="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!--  上段：アイコン列（Filesっぽく）  -->
                            <UniformGrid Margin="4,4,4,8" Columns="6">
                                <Button
                                    Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=CutCommand}"
                                    Style="{StaticResource Files.IconButton}"
                                    ToolTip="切り取り">
                                    <TextBlock FontSize="14" Text="✂" />
                                </Button>
                                <Button
                                    Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=CopyCommand}"
                                    Style="{StaticResource Files.IconButton}"
                                    ToolTip="コピー">
                                    <TextBlock FontSize="14" Text="⧉" />
                                </Button>
                                <Button
                                    Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=PasteCommand}"
                                    Style="{StaticResource Files.IconButton}"
                                    ToolTip="貼り付け">
                                    <TextBlock FontSize="14" Text="📋" />
                                </Button>
                                <Button
                                    Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=RenameCommand}"
                                    Style="{StaticResource Files.IconButton}"
                                    ToolTip="名前の変更">
                                    <TextBlock FontSize="14" Text="✎" />
                                </Button>
                                <Button
                                    Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ShareCommand}"
                                    Style="{StaticResource Files.IconButton}"
                                    ToolTip="共有">
                                    <TextBlock FontSize="14" Text="⤴" />
                                </Button>
                                <Button
                                    Command="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DeleteCommand}"
                                    Style="{StaticResource Files.IconButton}"
                                    ToolTip="削除">
                                    <TextBlock FontSize="14" Text="🗑" />
                                </Button>
                            </UniformGrid>

                            <Border
                                Grid.Row="1"
                                Height="1"
                                Margin="6,0,6,6"
                                Background="{StaticResource Files.Menu.Separator}" />

                            <!--  メニュー本体：スクロール  -->
                            <ScrollViewer
                                Grid.Row="2"
                                MaxHeight="520"
                                Padding="2"
                                HorizontalScrollBarVisibility="Disabled"
                                VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type cm:Win11MenuItemModel}">
                                            <ContentControl>
                                                <ContentControl.Style>
                                                    <Style TargetType="ContentControl">

                                                        <!--  通常メニュー  -->
                                                        <Setter Property="Content">
                                                            <Setter.Value>
                                                                <Button Command="{Binding Command}" Style="{StaticResource Files.MenuButton}">
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="*" />
                                                                            <ColumnDefinition Width="Auto" />
                                                                            <ColumnDefinition Width="Auto" />
                                                                        </Grid.ColumnDefinitions>

                                                                        <TextBlock Foreground="{StaticResource Files.Menu.Text}" Text="{Binding Text}" />

                                                                        <TextBlock
                                                                            Grid.Column="1"
                                                                            Margin="14,0,0,0"
                                                                            Foreground="{StaticResource Files.Menu.SubText}"
                                                                            Text="{Binding Gesture}" />

                                                                        <TextBlock
                                                                            Grid.Column="2"
                                                                            Margin="12,0,0,0"
                                                                            Foreground="{StaticResource Files.Menu.SubText}"
                                                                            Text="›"
                                                                            Visibility="{Binding HasSubmenu, Converter={StaticResource BooleanToVisibilityConverter}}" />


                                                                    </Grid>
                                                                </Button>
                                                            </Setter.Value>
                                                        </Setter>

                                                        <!--  セパレータ  -->
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                                <Setter Property="Content">
                                                                    <Setter.Value>
                                                                        <Border
                                                                            Height="1"
                                                                            Margin="6,6"
                                                                            Background="{StaticResource Files.Menu.Separator}" />
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </DataTrigger>
                                                        </Style.Triggers>

                                                    </Style>
                                                </ContentControl.Style>
                                            </ContentControl>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
