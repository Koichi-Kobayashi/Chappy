<Window
    x:Class="Chappy.Wpf.Controls.Test.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:Chappy.Wpf.Controls.Behaviors;assembly=Chappy.Wpf.Controls"
    xmlns:chappy="http://schemas.chappy.dev/wpf/controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Chappy.Wpf.Controls.Test"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="1280"
    Height="1600"
    mc:Ignorable="d">

    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <chappy:ColorPicker SelectedColor="{Binding MyColor}" ToolTipLanguage="en" />

        <DataGrid
            Grid.Column="1"
            behaviors:BoxSelectBehavior.IsEnabled="True"
            behaviors:DataGridDragDropBehavior.BuildPayload="{Binding BuildDragPayload}"
            behaviors:DataGridDragDropBehavior.DropHandler="{Binding OnDrop}"
            behaviors:DataGridDragDropBehavior.IsEnabled="True"
            behaviors:DataGridEditGuardBehavior.IsEnabled="True"
            behaviors:ShellContextMenuBehavior.CurrentFolderPath="{Binding CurrentFolder}"
            behaviors:ShellContextMenuBehavior.IsEnabled="True"
            behaviors:ShellContextMenuBehavior.PathPropertyName="FullPath"
            ItemsSource="{Binding Items}"
            RowHeight="28"
            ScrollViewer.CanContentScroll="True"
            SelectionMode="Extended"
            SelectionUnit="FullRow"
            VirtualizingPanel.IsVirtualizing="True"
            VirtualizingPanel.VirtualizationMode="Recycling" />

    </Grid>
</Window>
